package com;

import java.util.Scanner;

public class EntradaDeDatos {

	public static void main(String[] args) {
		// Introducir valores que puedan ser leidos desde el tteclado
		//Ejemplo guardar un valor numerico 
		
		
		//declaraos una variable donde guardar ese valor
		
/*		int edad;
		//creamos una variable de una clase llamada Scanner
		
		Scanner entrada = new Scanner (System.in);
		// antes de asignar un valor podemos mandar una impresion en consola
		//para solicitar datos
		
		System.out.println("Introduce una edad");
		
		edad = entrada.nextInt();
		// se puede seguir usando scanner y guardar nombre
		System.out.println("Intriduce tu Nombre: ");
		String nombre = entrada.next();
		
		// para no tener uso de memoria podemo cerrar el scanner
		
		entrada.close();
		
		//una vez guardado el valor podemos hacer uso de el para hacer una operacion
		
		System.out.println("El proximo año: " + nombre + " Tendras " + (edad+1));

		//operadpr Modulo %
		// nos ayuda a devolver el valor del residuo de una division
*/		
		int x= 10;
		int resultado =10%3;
		System.out.println(resultado);
		
		
	}

}
